<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Byte2Bites"
        tools:targetApi="31">

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- Launcher: Welcome / Auth entry -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Byte2Bites">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main host activity (ViewPager2 + bottom nav with Home/Orders/Profile fragments) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Auth screens -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />

        <!-- Profile / settings -->
        <activity
            android:name=".ChangeUserInfoActivity"
            android:exported="false" />
        <activity
            android:name=".AddressActivity"
            android:exported="false" />
        <activity
            android:name=".ChangePasswordActivity"
            android:exported="false" />

        <!-- Seller's products (buyer browsing one shop) -->
        <activity
            android:name=".SellerProductsActivity"
            android:exported="false" />

        <!-- Product details + add to cart -->
        <activity
            android:name=".ProductDetailsActivity"
            android:exported="false" />

        <!-- Cart / checkout -->
        <activity
            android:name=".CartActivity"
            android:exported="false" />

        <!-- VoIP signaling screen -->
        <activity
            android:name=".VoipCallActivity"
            android:exported="false" />

        <!-- Location picker for address -->
        <activity
            android:name=".LocationPickerActivity"
            android:exported="false" />

    </application>
</manifest>
